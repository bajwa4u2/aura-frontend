/// Non-web stub for web-page reload. The blocking-screen "Reload" button
/// is only shown for the `web-prod` distribution, so this stub is never
/// invoked on mobile/desktop. Kept so conditional imports compile.
void reloadWebPage() {
  // Intentional no-op.
}
