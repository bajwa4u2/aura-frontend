include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/_legacy/**
    - lib/screens/**
    - lib/widgets/**
    - lib/state/**
    - lib/test/**
    - test/**

linter:
  rules:
    # Keep your current lint pack behavior; we are just excluding dead trees.
    # No extra rules added here on purpose.
    {}
