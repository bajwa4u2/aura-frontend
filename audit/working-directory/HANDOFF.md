# Handoff - aura_final

Last updated: 2026-08-11 UTC (Takeover audit continuity reconciliation; Identity Foundation Phase 1)

Read this first, then `CURRENT_STATE.md`, then `AGENTS.md` (operating law).

Orientation:

- **Identity Foundation Phase 1 is COMPLETE, founder Gate 2 approved, committed `56a0bb7`, pushed to `origin/main` (paired backend `18b2cfb`).** Record: `../aura-backend/docs/2026-08-11-identity-foundation-phase-1-implementation.md`. New `identityBaselineCompleteProvider` mirrors `emailVerifiedProvider`; `router.dart` redirects to new `kCompleteIdentityRoute` (`/complete-identity`) checked before email verification; new `IdentityBaselineScreen` mirrors `VerifyPendingScreen`'s shape. `new_conversation_screen.dart`'s identity resolution now lives in canonical `lib/core/directory/directory_entry.dart` (`DirectoryEntry`/`memberEntryFromMap`/`dedupeDirectoryEntries`) — `id` always derives from `userId`, no more two independently-ordered fallback chains. **Second pass, 2026-08-11**: new `lib/core/directory/member_picker_field.dart` (`MemberPickerField`) wired into `institution_spaces_screen.dart` — institution-space creation now has member selection end to end, reusing the same canonical identity model rather than a second one. No age-eligibility threshold implemented (none exists; flag to founder if needed). `flutter analyze` clean, practical suite 147/147 (up from 137), web build succeeded. Meetings and every other certified authority untouched across both passes.
- **Continuity correction, 2026-08-11**: the three entries below previously read "pending founder Gate 2/commit authorization" or "do not commit/push without founder authorization." A takeover audit confirmed all three were already founder-approved, committed, and pushed — the doc tense was stale, not the underlying authorization. See `DECISIONS.md`'s matching entries.
- **Notification Delivery Authority Phase 1 is backend-only, Gate 2 approved, committed, and pushed** as `b23ff4419089483b8f5132f6ab4036d50ebb87ef` in `../aura-backend`. Record: `../aura-backend/docs/2026-08-09-notification-delivery-authority-phase-1-implementation.md`. No Flutter code changed. Existing `call:incoming`, direct Stage A push, canonical Communication-linked push, notification tap/deeplink behavior, `NotificationBridge`, `incomingCallBridgeProvider`, web service-worker behavior, and root Thread lifecycle ownership remain the client contract. Native background/terminated notification certification remains unresolved and mandatory before native production release.
- **Correspondence entry flow repair is implemented, founder Gate 2 approved, committed, and pushed** as `7a47fefe5924ea19a1e483475182d72383b10687` ("correspondence: preserve multi-member selection and creation flow"). Record: `docs/2026-08-08-correspondence-entry-flow-repair.md`. Founder evidence: selecting a second member in Messages -> Create removed/replaced the first and create failed with backend validation. Root cause: selected IDs survived but selected entries were derived from transient relationship/search results, so changing search results dropped prior selections from both UI chips and submitted payload. `NewConversationScreen` now keeps stable selected entries by ID, prevents duplicates, removes only the intended member, filters current user, and submits from the visible stable state. One selected member creates `PRIVATE`; multi-member/title creates shared space; Circle/Workroom/Salon are distinct. Submit errors use `AppErrorMapper`. `CompositionAssist` has a layout-only responsive fix for the embedded shared-space details surface. Certification: focused entry flow 5/5, practical non-golden 130/130, analyzer clean, web build successful. Backend paired contract repair is `9739ad5fb7551a0857ad22159add3e102eb7cc40` in `../aura-backend`, record `../aura-backend/docs/2026-08-08-correspondence-entry-flow-contract-repair.md`. **Note carried forward from the Identity Foundation Phase 1 audit (2026-08-11): this repair fixed selection stability, not identity resolution — selected members can still render/submit incorrect canonical identity; see the Identity Foundation section below.**
- **Device Communication Presence Phase 1 is backend-only, Gate 2 approved, committed, and pushed** as `3bf8d67976c230767e0e108788d67f670dd883e3` in `../aura-backend`. Record: `../aura-backend/docs/2026-08-08-device-communication-presence-phase-1-implementation.md`. No Flutter code changed. Current Flutter continues to consume the same REST join/decline endpoints and socket events. Backend now gates accepted/declined terminal synchronization by first-action-wins authority and uses existing `session:replaced` for losing Thread/DM media devices, which current Flutter already handles. Activity doctrine, Notification Delivery, native background notification handling, preferred-device policy, and transfer remain deferred.
- **Communication Continuity & Presence Chapter 1 contracts are frozen in backend.** Record: `../aura-backend/docs/2026-08-08-communication-continuity-presence-chapter-1-contracts.md`. Approved authorities: Communication Runtime Lifecycle, Notification Delivery, Device Communication Presence, Communication Timeline. Mandatory before Desktop/Android/iOS production release: Device Presence Phase 1, Notification Delivery Phase 1, native background/terminated notification certification, Communication Timeline Phase 1, iOS Firebase/APNs config confirmation, and Meetings preservation certification. No Flutter runtime behavior was changed by this chapter.
- **Communication Continuity & Presence platform architecture discovery is recorded in backend.** Record: `../aura-backend/docs/2026-08-08-communication-continuity-presence-platform-architecture.md`. Chapter 1 resolved the authority decisions and froze contracts. No Flutter runtime code was edited. Stage 1 remains the Thread foreground lifecycle seed, not a platform-wide authority. Meetings remain protected.
- **Pre-release connected-system health audit is complete.** Record: `docs/2026-08-08-pre-release-connected-system-health-audit.md`. Verdict: READY WITH NON-BLOCKING RISKS. App root/provider lifecycle, auth/session, notifications/attention, correspondence/Threads, realtime, Meetings, device registration, navigation/deep links, web, desktop, Android, backend contract boundary, and governance are healthy. iOS is AT RISK only because the CI-provided `FIREBASE_IOS_CONFIG_BASE64` secure value cannot be verified from Windows and must be confirmed before/with the iOS build pipeline. No runtime code was edited during the audit.
- **Canonical Flutter Thread-Call Lifecycle Stage 1 is implemented, locally certified, committed, and pushed.** Commit `86de6e165931e96185a2e78a349bb5502065940a` (`realtime: establish canonical thread call lifecycle`). New files: `lib/features/realtime/application/thread_call_lifecycle_controller.dart`, `lib/features/realtime/presentation/thread_call_lifecycle_host.dart`, and `test/thread_call_lifecycle_controller_test.dart`. Root mount is in `AuraApp`; shell-local incoming overlays were removed from `MemberShell`/`InstitutionShell`; Thread caller start, Thread call-card join, and overlay accept delegate to the lifecycle owner while `RealtimeController` remains the execution authority. Foreground FCM call interrupts feed the same bridge/dedup path. Existing external/background `/realtime/:sessionId?action=join` route behavior remains unchanged because shared realtime route edits were not authorized/needed for foreground Stage 1. Activity, multi-device arbitration, and platform background/native push remain deferred. No backend code, schema, shared realtime controller/state/socket/media files, or Meetings product files were edited.
- **Stage 1 Gate 2 toolchain health:** original Flutter hangs traced to SDK Git safe-directory/tool startup for `C:/flutter`. Use `GIT_CONFIG_COUNT=1`, `GIT_CONFIG_KEY_0=safe.directory`, `GIT_CONFIG_VALUE_0=C:/flutter` for local Flutter certification commands in this sandbox. Latest results: focused Stage 1 6/6, relevant set 31/31, non-golden suite 125/125, `flutter analyze` clean, web release build successful.
- **Governance addendum is active platform-wide.** Certified surfaces are protected by default. Solve within the owning feature/module before touching shared systems. Any shared-system touch must identify the boundary, preserve existing behavior, run targeted regression, certify health, and report it separately. If preservation is impossible, stop for founder approval.
- **Canonical Flutter Thread-Call Lifecycle Stage 1 blueprint is implemented.** Blueprint/design record: `docs/2026-08-08-canonical-flutter-thread-call-lifecycle-stage-1-blueprint.md`. Stage 1 remains a Flutter chapter combining foreground `call:incoming` ownership and caller/callee realtime synchronization. Activity doctrine, multi-device arbitration, and platform background/native notifications are still deferred. Meetings must remain exactly unchanged.
- **Thread Calling Reliability collective audit is current.** Authoritative record: `../aura-backend/docs/2026-08-08-thread-calling-reliability-collective-audit.md`. New founder evidence: Chrome background notification works; Desktop foreground and iOS foreground show no incoming-call interruption/no Activity entry; Android previously showed no interruption/notification despite backend FCM `SENT`; Party B accepted/participated while Party A remained on `Connecting...`. Do not treat this as isolated push-only work. Current Flutter risk domains are app/shell-level incoming-call consumer ownership, Activity ledger mismatch, visible `RealtimeController` join-state synchronization before `joinState=joined`, and multi-device replacement/terminal synchronization. Backend Reachability, Session Continuity Phase 1, and Stage A canonical notification normalization remain closed; Meetings must be preserved.
- **Post integrity remediation (2026-07-21):** member compose, public composer, institution compose, edit, and draft-resume flows now share the same rule: top-level publishing/saving requires selected canonical topic state. Raw `#` text is not topic selection. Draft discard calls the backend draft-delete endpoint and edit mode bypasses draft autosave.
- **Post editing uses canonical composers.** Member edit routes to `ComposeScreen(editPostId: ...)` and saves through `PUT /posts/:id`; institution edit hydrates the existing row via `GET /institutions/:institutionId/posts/:postId` and saves through `PATCH`, preserving the post id and metadata unless changed.
- **Governed tag persistence remediation (2026-07-21):** `GovernedTagAutocomplete` owns canonical `TagReference` callbacks. Member posts, institution posts/replies, announcements, and correspondence messages submit `tagReferences` (plus legacy `mentions` for compatibility). The editor inserts display-name tokens, payloads persist canonical entity ids, renderers resolve current display names, and handles/slugs remain secondary context/routing only.
- **Governed tagging** (`lib/core/tagging/`) is reusable infrastructure. Wrap a composer's `TextEditingController`/`FocusNode` in `GovernedTagAutocomplete`; do not build a new mention widget. `TagKind` is open; add entity kinds there, not a parallel system.
- **Identity rendering** always delegates to `AuraAvatar` for photo/logo/avatar/initials/placeholder precedence. If a surface shows initials despite a real image, first verify whether the API payload includes `avatarUrl`/`logoUrl`.
- **Module attention** (`lib/features/updates/module_attention.dart`) is a pure projection over the notifications the global bell already polls. Never add a second unread-count source for a module.
- Pre-join is resolver-driven: the backend's participation resolver returns an outcome; `meeting_entry_resolution.dart` maps outcomes to UI states. Never add a client-side identity form at a meeting door.
- The WebRTC engine (`lib/features/realtime/`) survived reliability hardening on 2026-07-10. Understand that design before touching reconnection logic.
- Latest remediation verification: `flutter analyze` clean. Targeted (`test/governed_tag_persistence_test.dart`) and full `flutter test -r expanded` invocations hung in this Windows workspace before output; treat as an environment/tooling blocker until a normal runner completes.
- ROS Phase II audit deliverables and closeout live in `representation/inventory/` in this repo.
- The backend is `../aura-backend` (own repo, own continuity set).

Pending and founder gates: `NEXT_WORK.md`.
